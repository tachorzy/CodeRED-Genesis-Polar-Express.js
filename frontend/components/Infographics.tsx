import Image from 'next/image'
import React, { useState } from 'react'
// import { chillaxRegular } from '@/utils/localNextFont'

const Infographics = () => {
    return( 
        <div className={`flex flex-col pr-16 mt-48 gap-y-4`}>
            <h1 className={"text-[#FCFFFF] text-4xl"}>{"Want cheap flights but can't find anything but red-eyes and long layovers?"}</h1>
            <h1 className={"text-[#FCFFFF] text-4xl"}>{"Slay your layover. Gain with Slayover."}</h1>
            <div className={"flex flex-row"}>
            </div>
        </div>
    );
}

export default Infographics;